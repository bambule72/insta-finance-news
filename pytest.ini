[pytest]
# Filter out deprecation warnings from third-party libraries
filterwarnings =
    # Ignore feedparser's use of deprecated 'cgi' module (will be fixed in feedparser updates)
    ignore:.*'cgi' is deprecated.*:DeprecationWarning:feedparser.encodings
